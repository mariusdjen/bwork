# ============================================
# B-WORK — Variables d'environnement
# ============================================
# Copier ce fichier en .env.local et remplir les valeurs

# --- Supabase (client + serveur) ---
# URL du projet Supabase (visible dans Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# Cle publique anon (visible dans Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Cle service role (SERVEUR UNIQUEMENT — jamais cote client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- AI Providers (SERVEUR UNIQUEMENT) ---
# Au moins un provider requis pour la generation
# Provider par defaut : anthropic (configurable via AI_PROVIDER)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_GENERATIVE_AI_API_KEY=AI...
GROQ_API_KEY=gsk_...

# --- AI Configuration (optionnel) ---
# Provider : anthropic | openai | google | groq (defaut: anthropic)
# AI_PROVIDER=anthropic
# Modele (defaut depend du provider : claude-sonnet-4-20250514, gpt-4o, gemini-2.0-flash, llama-3.3-70b-versatile)
# AI_MODEL=claude-sonnet-4-20250514

# --- Stripe (SERVEUR + CLIENT) ---
# Cle secrete Stripe (SERVEUR UNIQUEMENT)
STRIPE_SECRET_KEY=sk_test_...
# Secret de verification des webhooks Stripe (SERVEUR UNIQUEMENT)
STRIPE_WEBHOOK_SECRET=whsec_...
# Cle publique Stripe (visible cote client)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
# Price IDs des plans (depuis Stripe Dashboard > Products)
NEXT_PUBLIC_STRIPE_PRO_PRICE_ID=prod_Ttt0ER0qCHPZJ2
NEXT_PUBLIC_STRIPE_BUSINESS_PRICE_ID=prod_Ttt18lbzivLAfX

# --- Application ---
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Sandbox Configuration (SERVEUR UNIQUEMENT) ---
# Provider selection: e2b | vercel | auto (defaut: auto)
# auto = essaie E2B d'abord, fallback sur Vercel si echec
SANDBOX_PROVIDER=auto

# E2B Sandbox (provider principal recommande)
# Obtenez votre cle sur https://e2b.dev
E2B_API_KEY=e2b_...

# Vercel Sandbox (provider alternatif)
# Option 1: OIDC Token (recommande pour deployments Vercel)
# VERCEL_OIDC_TOKEN=...
# Option 2: Personal Access Token (pour dev local)
# VERCEL_TOKEN=...
# VERCEL_TEAM_ID=team_...
# VERCEL_PROJECT_ID=prj_...
