services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: open-lovable-prod
    ports:
      - "3001:3001"
    env_file:
      - .env
    restart: unless-stopped
